@if (!string.IsNullOrWhiteSpace(Model?.Subject?.Value) || !string.IsNullOrWhiteSpace(Model?.FurtherInformation?.Value) || !string.IsNullOrEmpty(Model?.EntryRequirementPreface) || Model?.EntryRequirements.FirstOrDefault() != null || Model?.AdditionalInformation.FirstOrDefault() != null)
{
    <section class="job-profile-subsection" id="@Model.RouteName.ToString()">
        <h3>@Model.RouteName.ToString()</h3>
        <div class="job-profile-subsection-content">

            @Html.Raw(Model?.Subject)

            @Html.Raw(Model?.FurtherInformation)

            @if (Model?.EntryRequirements?.Count() > 0 || !string.IsNullOrWhiteSpace(Model.EntryRequirementPreface))
            {
                <h4>Entry requirements</h4>
                <p>@Model.EntryRequirementPreface</p>

                <ul class="list-reqs">
                    @foreach (var requirement in Model.EntryRequirements)
                    {
                        <li>@Html.Raw(requirement)</li>
                    }
                </ul>
            }

            @if (Model?.AdditionalInformation?.Count() > 0 )
            {
                <h4>More Information</h4>
                <ul class="list-link">
                    @foreach (var informationLink in Model.AdditionalInformation)
                    {
                        <li><a class="htb-feature" href="@informationLink.Link">@informationLink.Text</a></li>
                    }
                </ul>
            }
        </div>
    </section>
}

