@model DFC.App.JobProfiles.HowToBecome.ViewModels.IndexViewModel

<html>
<body>
    @if (Model.Documents?.Count() > 0)
    {
        <ul>
            @foreach (var document in Model.Documents)
            {
                <li>
                    <a href="/segment/@document.CanonicalName">@document.CanonicalName</a>
                </li>
            }
        </ul>
    }
</body>
</html>